/* CDbxStub exports no headers of its own — it only provides the symbols
 * declared in CDbxFFI/include/dbx_ffi.h. This file exists because SwiftPM
 * requires a public-headers directory for every C target. */
#ifndef DBX_STUB_H
#define DBX_STUB_H
#endif
