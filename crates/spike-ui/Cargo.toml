# STUB package. The real UI spikes live one level deeper, each its own
# standalone cargo workspace (own [workspace] table + own Cargo.lock) so
# their dependency trees never touch the shared datagrep workspace lockfile:
#   ./harness/       gpui 0.2.2 + gpui-component 0.5.1 (S1, S8 as designed)
#   ./harness-winit/ bare winit 0.30 + wgpu 30 (S1 fallback -- gpui could not
#                     be built on this machine, see SPIKE-REPORT.md)
# This stub only exists because the parent workspace's `crates/*` member glob
# requires a manifest here, and a member with its own [workspace] table would
# break the parent with "multiple workspace roots found" (verified
# empirically -- see SPIKE-REPORT.md).
#
# Build:   cd harness && cargo build --release        (fails on this machine)
#          cd harness-winit && cargo build --release  (succeeds)
# Results: ./SPIKE-REPORT.md
#
# CARGO_TARGET_DIR=/Users/nurchudlori/Projects/dbx/target-spike was used for
# every build below, per the task's isolation instructions.
[package]
name = "spike-ui"
version = "0.0.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[dependencies]
