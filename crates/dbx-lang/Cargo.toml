[package]
name = "dbx-lang"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Per-language editor support for dbx: statement splitting, classification, minimal edit context, and highlighting. Hand-rolled lexers only (design §3.6) — no parser-generator, no tree-sitter, no sqlparser-rs."

[dependencies]
dbx-api = { path = "../dbx-api" }
thiserror = { workspace = true }
