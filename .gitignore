/target
# per-crate build dirs used to avoid cargo lock contention
/target-*
.build
**/*.rs.bk
*.pdb
.DS_Store
ui/macos/.build
ui/macos/.bench
ui/macos/*.app
# scripts/make_dmg.sh output (same layout the release workflow uses)
/dist
